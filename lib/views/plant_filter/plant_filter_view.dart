import 'package:flutter/material.dart';

class PlantFilterView extends StatelessWidget {
  const PlantFilterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
